var typed= new Typed(".text", {
    strings:["Basketball player sometimes", "Handsome sabi ni mama", "Humble ferson", "Multi talented (eme)",],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop:true
});
